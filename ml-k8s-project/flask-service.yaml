apiVersion: v1
kind: Service
metadata:
  name: flask-1  # Wahi naam jo aapke Nginx ConfigMap mein hai
spec:
  selector:
    app: flask-app
  ports:
    - protocol: TCP
      port: 5000
      targetPort: 5000